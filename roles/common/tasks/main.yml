---
# tasks file for roles/common
- name: Install common packages
  dnf:
    name:
      - vim
      - git
      - wget
      - curl
    state: present

- name: Create project directory
  file:
    path: /opt/company
    state: directory
    mode: '0755' 
